/**
 * Converts a number a string.
 * @param {number} n
 * @return {string} the number as a string
 */
 	function numToString(num) {
 		return String(num);
 	}
 		numToString (3);


/**
 * Adds one to a given number.
 * @param {number} n
 * @return {number}
 */
function number (n) {
	return (n+1);
}
number (5);

/**
 * Subtracts one from a given number.
 * @param {number} n
 * @return {number}
 */function number (n) {
 	 return (n-1);
 }
 	number (5);


/**
 * Adds two numbers.
 * @param {number} x
 * @param {number} y
 * @return {number} the sum
 */
function number (X,Y) {
	return (X+Y);
}
	number (3,4);

/**
 * Subtracts the second number from the first.
 * @param {number} x
 * @param {number} y
 * @return {number} the difference
 */
 function number (X,Y) {
	return (X-Y);
}
	number (7,4);

/**
 * Multiplies two numbers.
 * @param {number} x
 * @param {number} y
 * @return {number} the product
 */
 	 function number (X,Y) {
	return (X*Y);
}
	number (5,4);


/**
 * Divides the first number by the second.
 * @param {number} x
 * @param {number} y
 * @return {number} the quotient
 */
 	function number (X,Y) {
	return (X/Y);
}
	number (10,5);

/**
 * Multiplies a number by itself.
 * @param {number} x, number to be squared
 * @return {number} squared
 */function squared (num) {
 		return num*num;
 }
 	squared (5);


/**
 * Performs a mathematical operation on two numbers.
 * Also prints out the equation: (i.e.) "1 + 5 = 6" or "8 / 2 = 4".
 * @param {string} operation "add", "subtract", "multiply", or "divide"
 * @param {number} x
 * @param {number} y
 * @return {number} the result
 */


/**
 * Returns true if `a` is greater than `b`.
 * @param {number} a
 * @param {number} b
 * @return {boolean} `a` is larger than `b`
 */function greaternumber (a,b){
 		if(a>b){
 			return console.log("a is greater");
 		}
 		else
 			return console.log("a is smaller");
 			
 }
  greaternumber(4,6);


/**
 * Returns true if `a` is less than `b`.
 * @param {number} a
 * @param {number} b
 * @return {boolean} `a` is smaller than `b`
 */
 function boolean (a,b){
 		if(a<b){
 			return console.log("true");
 		}
 		else
 			return console.log("false");
 			
 }
  boolean(4,6);

/**
 * Returns true if `a` and `b` are equal.
 * @param {number} a
 * @param {number} b
 * @return {boolean} the numbers are equal
 */
  function equal (a,b){
 		if(a=b) {
 			return true;
 		}
 		else (a!=b) {
 			return false;
 		}
 			
 }
  	equal(6,6);

/**
 * Returns the smallest value of two numbers.
 * @param {number} x
 * @param {number} y
 * @return {number} the smallest number
 */ function smallerNumber (x,y){
 		if(x<y) {
 			return console.log("x is smallest");
 		}
 		else {
 			return console.log("x is largest");
 		}
 			
 }
  	smallerNumber(6,12);


/**
 * Returns the largest value of two numbers.
 * @param {number} x
 * @param {number} y
 * @return {number} the largest number
 */
 function largestNumber (x,y){
 		if(x>y) {
 			return console.log("x is greater No");
 		}
 		else {
 			return console.log("x is smallest No");
 		}
 			
 }
  	largestNumber(60,12);

/**
 * Returns true if `n` is even.
 * @param {number} n
 * @return {boolean} the number is even
 */
 	function even (n){
 		if (n%2==0){
 			return console.log("is even number")
 		}
 		else{
 			return console.log("is odd number")
 		}
 	}
 		even(10);


/**
 * Returns true if `n` is odd.
 * @param {number} n
 * @return {boolean} the number is odd
 */
function odd (n){
 		if (n%2==1){
 			return console.log("is odd number")
 		}
 		else{
 			return console.log("is even number")
 		}
 	}
 		odd(11);

/**
 * Returns a letter grade.
 * "A": 90-100%
 * "B": 80-89%
 * "C": 70-79%
 * "D": 60-69%
 * "F": 0-59%
 * @param {number} score
 * @param {number} total maximum possible score
 * @return {string} the score represented as a letter grade
 */


/**
 * Checks if a `restaurant` object has a `reviews` property.
 * If it does, increase the property's `reviews` value by 1.
 * If it does not, set the `reviews` value to 1.
 * @param {object} restaurant   represents a restaurant object
 * @return {object} restaurant
 */


/**
 * Joins two strings with a space.
 * @param {string} word1
 * @param {string} word2
 * @return {string} joined the words joined with a space
 */


/**
 * Returns a circle object with the properties `circumference` and `area`.
 * Use Math.PI for the value π.
 * (https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/PI)
 * @param {number} radius
 * @return {object} circle
 */

